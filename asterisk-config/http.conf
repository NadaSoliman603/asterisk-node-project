; ==========================================================
; http.conf — enables the built-in HTTP server that ARI rides on.
; ==========================================================

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
prefix=
; TLS is disabled for local dev. Do NOT expose 8088 publicly without it.
tlsenable=no
