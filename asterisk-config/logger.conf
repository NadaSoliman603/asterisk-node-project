; ==========================================================
; logger.conf — logs to stdout (docker logs) + /var/log/asterisk/full.
; ==========================================================

[general]
dateformat=%F %T

[logfiles]
console => notice,warning,error,verbose(3)
full => notice,warning,error,debug,verbose(3),dtmf
