; ==========================================================
; rtp.conf — matches the 10000-10100/udp range published in docker-compose.
; ==========================================================

[general]
rtpstart=10000
rtpend=10100
