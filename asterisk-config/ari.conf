; ==========================================================
; ari.conf — ARI (Asterisk REST Interface) users.
; Credentials must match ARI_USER / ARI_PASSWORD in ../.env.
; ==========================================================

[general]
enabled=yes
pretty=yes
; Allow origin for local browser tooling; not required for the Node client.
allowed_origins=*

[ariuser]
type=user
read_only=no
password=fgGo6A62TwSlf6rmzN2NTKEX4Jlo7WM4
password_format=plain
