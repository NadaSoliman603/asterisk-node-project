; ==========================================================
; modules.conf — autoload everything, block chan_sip explicitly.
; ==========================================================

[modules]
autoload=yes

; chan_sip is deprecated; the spec forbids it. chan_pjsip is used instead.
noload => chan_sip.so
